#include <stdio.h>

/**
 * main - prints alphabets in lowercase.
 * 
 * Return: Always 0 (Success)
 */
int main(void)
{
	char z;

	for (z = 'x'; z <= 'y'; z++)
	{
		putchar(z);
	}
	putchar('\n');
	return (0);
}
